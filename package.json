{
  "name": "amazon-sp-api-data-downloader",
  "version": "1.0.0",
  "description": "Amazon SP-API data downloader with tests",
  "main": "getSPAPIdata.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "jest": "^29.7.0"
  }
}
